import { StyleSheet } from 'react-native';

const globalStyles = StyleSheet.create({
    errorText: {
        color: 'crimson',
        fontWeight: 'bold',
        marginBottom: 2,
        marginTop: 2,
        textAlign: 'center',
    },
});

export default globalStyles;